<Window x:Class="To_do_List.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:To_do_List"
        xmlns:to_do_list="clr-namespace:To_do_List"
        mc:Ignorable="d"
        Title="To-Do List" Height="450" Width="800"
        ResizeMode="CanMinimize"
        Background="CadetBlue">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel>
            <Calendar x:Name="Calendar" Foreground="AliceBlue" Background="LightGray" SelectedDatesChanged="Calendar_SelectedDatesChanged"/>
            <Label Content="Progres" FontSize="15" Foreground="LightGray" HorizontalAlignment="Center"/>
            <ProgressBar x:Name="ProgressBar" Height="20" Value="0" Maximum="100" Margin="0,5"/>
            <Button x:Name="Add_btn" Content="Add" Background="LightGray" Click="Add_btn_Click"/>
            <Button x:Name="Save_btn" Content="Save" Background="LightGray" Click="Save_btn_Click"/>
            <Button x:Name="Load_btn" Content="Load" Background="LightGray" Click="Load_btn_Click"/>
        </StackPanel>

        <Grid Grid.Column="1" Grid.ColumnSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Label Content="To-do" Grid.Row="0"/>
            
            <StackPanel x:Name="todolist_stack_panel" Grid.Row="0" Margin="5, 15" />

            <Label Content="Completed" Grid.Row="1"/>

            <StackPanel x:Name="todolist_completed_stack_panel" Grid.Row="1" Margin="5, 15"/>
        </Grid>
            
    </Grid>
</Window>
